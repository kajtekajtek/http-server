#include "http.h"

struct http_request parse_request(uint8_t *raw_message)
{
	struct http_request req;

//	req.method = get_method(raw_message);
//	req.path = get_path(raw_message);
//	...

	return req;
}
